import { ThyFormatPipe } from './thy-format.pipe';

describe('ThyFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ThyFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
