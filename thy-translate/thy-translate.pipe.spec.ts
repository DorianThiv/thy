import { ThyTranslatePipe } from './thy-translate.pipe';

describe('ThyTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ThyTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
